<!DOCTYPE html>
<html>
  <head>
    <title>事前課題</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://unpkg.com/ress/dist/ress.min.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header">
      <span class="title">
        <a href="/">MiniBlog-事前課題-</a>
      </span>
      <% if user_signed_in? %>
        <span class="menu-bar">
          <span><%= link_to "投稿", new_blog_path, method: :get, class: "menu" %></span>
          <span><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "menu" %></span>
          <span><%= link_to "マイページ", user_path(current_user.id), method: :get, class: "menu" %></span>
        </span>
      <% else %>
        <span class="menu-bar">
          <span><%= link_to "ユーザー登録", new_user_registration_path, method: :get, class: "menu" %></span>
          <span><%= link_to "ログイン", new_user_session_path, method: :get, class: "menu" %></span>
        </span>
      <% end %>
    </div>
    <div class="contents">
      <%= yield %>
    </div>
    <div class="footer">
      <small>© 2020 ak_programing inc.</small>
    </div>
  </body>
</html>
